<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m" xmlns:f="sap.ui.layout.form">
    <f:SimpleForm>
        <f:content>
            <Label text="name" />
            <Text text="{name}" />
            <Label text="availability" />
            <Text text="{stockStatus}" />
            <Label text="price" />
            <Text text="{price}" />
            <Label text="currency" />
            <Text text="{currency}" />
            <Label text="health" />
            <Text text="{healthBenefits}" />
        </f:content>
    </f:SimpleForm>
</core:FragmentDefinition>
